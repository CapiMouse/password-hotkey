[project]
name = "02-new-password-sas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "keyboard>=0.13.5",
    "pysimplegui>=6.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
